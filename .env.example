# Copy to .env and fill in. .env is gitignored and must never be committed.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GMAIS_LLM_BASE_URL=https://api.openai.com/v1
GMAIS_LLM_MODEL=gpt-4o-mini
# Pin the web console to the offline backend (no API calls, no cost):
GMAIS_DEMO_MODE=1
